/*Convert this into your language rule syntax.*/

walangbalikan starthere() {
	digeetz x = 0;
	sabaw _veryWeirdIdentifier567 = 999.9f;
	weh y = (5 > 4 AND yep) OR (false AND NOT(x==0));
	digeetz#10 myArray;
	
	allchat("What is the input: "); 
	typehere(x); //ask for user input
	allchat("Input: " ,x) //print the input
	
	bestloopimo(digeetz i = 0; i < 5; i++) {
		allcat("Hello world");
	}
}