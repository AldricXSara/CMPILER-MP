/*Convert this into your language rule syntax.*/

digeetz myFunction(digeetz x) {
	digeetz add = x + 50 ++ 1; /*syntax error #1*/
	
	uwina int; //syntax error #2
}

sabaw myFunctionFloat(sabaw x) {
	sabaw fTest = (x * 50) + (x * 15)); //syntax error #3
	
	uwina fTest * 50 (); //syntax error #4
}

walangibabalik starthere() {
	myFunction(50);
	myFunctionFloat(50.0f);
}