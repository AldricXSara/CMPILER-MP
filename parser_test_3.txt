/*Convert this into your language*/

walangibabalik testOne() {
	digeetz x = 0;
	allchat("Input a number: ");
	typehere(x);
	bestloopimo(digeetz i; i < 10; i += 1) { //syntax error #1
		allchat("Hello " +x+); //syntax error #2
	 
	
	//syntax error #3 (missing curly brace)
}

walangibabalik testTwo(sabaw x) {
	ha(x = 4)//syntax error #4 
	{
		allchat(x is a very huge number); //syntax error #5
	} 
}

walangibabalik starthere() {
	sabaw value = (5 * 1) + (5 * 5 / 1 + 3 + (4 + (5 * 3)))/2.0f * 8.0f;
	
	testOne()(x); //syntax error #6
	testTwo(123 ++value); //syntax error #7
}